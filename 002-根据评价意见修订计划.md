# 002-根据评价意见修订计划

## 内容摘要
- 阅读了 `revise.md` 中的专家建议。
- 决定采用更高效的方案：**放弃 U-Boot**，直接用 OpenSBI 引导内核。
- 强化了技术实现细节：改用 **ext4 磁盘镜像** 代替 initramfs，以更贴合真实存储系统。
- 明确了内核配置中必须开启的 **VirtIO** 相关参数。
- 更新了 `task-ai.md` 中的任务大纲。

## 下一步计划
- 继续执行环境准备，确保交叉编译器和 QEMU 可用。
- 开始下载并编译 OpenSBI。
