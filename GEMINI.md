我正在完成一个任务，任务的描述在 task.md 里面。
其中该目录(不包括子目录)的所有md文件都是预先写好的提示词或者之前的ai生成的中间阶段性总结，在工作前都必须仔细阅读。

同时把阶段性总结写到该目录的md里面，生成良好的序号以及内容摘要。格式为 001-内容摘要.md

在对话中保持使用中文。

---
**授权信息**：用户已授权在需要时使用 `sudo` 安装依赖，密码为 `qwer`。